[pytest]
DJANGO_SETTINGS_MODULE = core.settings
python_files = tests.py test_*.py

markers =
    integration: teste que faz chamada de rede real (ex: Mapbox). Pulado por padrão — rode com --run-integration.
